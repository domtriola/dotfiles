return {
  {
    "folke/sidekick.nvim",
    opts = {
      nes = { -- Next Edit Suggestions
        -- Disabled for now, so I can build a stronger intuition while learning new languages.
        -- When copilot suggestions are desired again re-enable this for nes
        -- and re-install the ai.copilot plugin for popup suggestions.
        enabled = false,
      },
    },
  },
}
